apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: biyi-gateway
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
  - hosts:
    - '*.yg.com'
    - yg.com
    port:
      name: http
      number: 80
      protocol: HTTP
