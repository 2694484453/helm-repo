apiVersion: v1
kind: ConfigMap
metadata:
  name: {{.Release.Name}}-cm
data:
  config.yaml: |-
    bind-addr: 127.0.0.1:8080
    auth: none #取消认证
    password:  # Randomly generated for each config.yaml
    cert: false
