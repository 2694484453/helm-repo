apiVersion: batch/v1
kind: Job
metadata:
  name: my-job-demo
spec:
  template:
    metadata:
      name: my-job-demo
    spec:
      containers:
        - name: my-job-demo
          image: busybox
          imagePullPolicy: IfNotPresent
          command: [ "sh","-c","echo '123-hahaha'" ]
      restartPolicy: Never
